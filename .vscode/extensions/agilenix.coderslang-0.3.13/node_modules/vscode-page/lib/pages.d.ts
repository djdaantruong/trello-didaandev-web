import * as vscode from "vscode";
export declare type MessageHandler = (parameters?: any) => Promise<any>;
export declare type Template = {
    id: string;
    content?: string;
    contentUrl?: string;
};
export interface MesssageMaping {
    command: string;
    handler: MessageHandler;
    templates?: Template[];
    forward?: string;
}
export declare function createOrShowPage(name: string, viewType: string, title: string, base: string, page: string, context: vscode.ExtensionContext, messageMappings: MesssageMaping[]): void;
